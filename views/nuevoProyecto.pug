extends layaut

include mixin/crearProyecto.pug

block contenido
  .contenido-principal
     
    h1 #{nombrePagina}

    if errores
      each error in errores
        .alerta= error.Texto
    +crearProyecto(proyecto)